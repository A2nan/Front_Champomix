import { UniqueCompaniesPipe } from './unique-companies.pipe';

describe('UniqueCompaniesPipe', () => {
  it('create an instance', () => {
    const pipe = new UniqueCompaniesPipe();
    expect(pipe).toBeTruthy();
  });
});
